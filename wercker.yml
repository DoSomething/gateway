box: dosomething/ds-docker-php

build:
  steps:
    - script:
        name: install sqlite
        code: sudo apt-get update && sudo apt-get install php7.0-sqlite3
    - leipert/composer-install
    - script:
        name: run phpunit tests
        code: vendor/bin/phpunit
